export class Maquinaria {
  constructor({ id, nombre, tipo, estado, ubicacion, capacidadTrabajo }) {
    this.id = id;                    
    this.nombre = nombre;               
    this.tipo = tipo;                    
    this.estado = estado;                
    this.ubicacion = ubicacion;          
    this.capacidadTrabajo = capacidadTrabajo; 
  }
}
